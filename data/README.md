# Local FUSeg data

This directory is intentionally excluded from Git except for this notice. Run
`scripts/download_data.py` to create the pinned sparse checkout and gitignored manifests.
Do not commit or redistribute FUSeg images, masks, image-level manifests, or derived images.
